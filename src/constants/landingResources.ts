import { LANDING_MODULES, LANDING_MODULES_HEADING } from './landingModules';

/** @deprecated use LANDING_MODULES — mantido para imports existentes. */
export const LANDING_RESOURCES = LANDING_MODULES;

export const LANDING_RESOURCES_HEADING = LANDING_MODULES_HEADING;
